cd /sag/be1015/IntegrationServer/instances/default/packages/WmDeployer/bin

# 1. List deployment reports (latest deployment status)
./Deployer.sh --list -candidate deploymentReport -dc terst -project terst -host 127.0.0.1 -port 5555 -user MElarief -pwd 'MElarief'

# 2. Show deployment report details (change -id if needed)
./Deployer.sh --showReport -candidate deploymentReport -dc terst -project terst -id 1 -host 127.0.0.1 -port 5555 -user MElarief -pwd 'MElarief'

# 3. List deployment candidates and status
./Deployer.sh --list -candidate dc -project terst -host 127.0.0.1 -port 5555 -user MElarief -pwd 'MElarief'

# 4. Check project information
./Deployer.sh --list -candidate project -host 127.0.0.1 -port 5555 -user MElarief -pwd 'MElarief' | grep terst

# 5. List deployed build artifacts/packages
./Deployer.sh --list -candidate build -project terst -host 127.0.0.1 -port 5555 -user MElarief -pwd 'MElarief'

# 6. List available checkpoints
./Deployer.sh --list -candidate checkpoint -project terst -host 127.0.0.1 -port 5555 -user MElarief -pwd 'MElarief'